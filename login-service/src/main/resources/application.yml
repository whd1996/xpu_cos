server:
  port: 8080
spring:
  application:
    name: login-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xpu_cos?serverTimezone=GMT%2B8
    username: root
    password: 123456
  resources:
    static-locations: classpath:/static
