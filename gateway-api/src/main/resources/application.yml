server:
  port: 7000
spring:
  application:
    name: gateway-api
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    gateway:
      discovery:
        locator:
          enabled: true

